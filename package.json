{
  "name": "hospital-equipment-management",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && node ../copy-dist.js",
    "vercel-build": "cd frontend && npm install && npm run build && node ../copy-dist.js"
  },
  "engines": {
    "node": "18.x"
  }
}
